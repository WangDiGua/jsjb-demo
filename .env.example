# 大模型（OpenAI 兼容 Chat Completions，经 Vite 代理 /glm-api）
# 复制为 .env.local 后填写密钥；*.local 已被 git 忽略。

# 阿里云 DashScope 兼容模式（默认）
# 文档：https://help.aliyun.com/zh/model-studio/use-qwen-by-calling-api
VITE_GLM_PROXY_TARGET=https://dashscope.aliyuncs.com
VITE_GLM_PROXY_PREFIX=/compatible-mode/v1
VITE_GLM_API_KEY=
# 模型名以控制台为准，例如：qwen-flash、qwen3-max-preview、qwen3.5-flash 等
VITE_GLM_MODEL=qwen3.5-flash

# 大模型 HTTP 超时（毫秒），防止代理/API 无响应时表单长时间卡在「提交中」。不设置时默认 45000
# VITE_GLM_TIMEOUT_MS=45000

# Mock 数据层：统一模拟接口延迟（毫秒）。留空或为 0 时列表/详情/保存等本地读写几乎无等待；演示「网络感」时可设为 200～400
# VITE_MOCK_LATENCY_MS=0

# —— 若改用 Moonshot Kimi，可改为：——
# VITE_GLM_PROXY_TARGET=https://api.moonshot.cn
# VITE_GLM_PROXY_PREFIX=/v1
# VITE_GLM_API_KEY=
# VITE_GLM_MODEL=kimi-k2-turbo-preview
